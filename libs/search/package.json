{
  "name": "@nextgen/search",
  "version": "1.0.0",
  "description": "NextGen Search Abstraction Layer - Multi-Provider Full-Text Search",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "license": "MIT",
  "scripts": {
    "test": "npx vitest run --config vitest.config.ts --passWithNoTests",
    "test:watch": "npx vitest --config vitest.config.ts",
    "test:coverage": "npx vitest run --config vitest.config.ts --coverage"
  },
  "dependencies": {
    "@elastic/elasticsearch": "^8.12.0",
    "meilisearch": "^0.37.0",
    "typesense": "^1.7.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.0"
  },
  "peerDependencies": {
    "@nestjs/common": "^10.0.0",
    "@prisma/client": "^5.0.0"
  }
}
